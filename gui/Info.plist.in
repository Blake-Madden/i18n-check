<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleExecutable</key>
	<string>${CMAKE_PROJECT_NAME}</string>

	<key>CFBundleDisplayName</key>
	<string>${quneiform_DESCRIPTION}</string>

	<key>CFBundleSpokenName</key>
	<string>${quneiform_DESCRIPTION}</string>

	<key>CFBundleGetInfoString</key>
	<string>${quneiform_DESCRIPTION} version ${quneiform_VERSION_MAJOR}.${quneiform_VERSION_MINOR}.${quneiform_VERSION_PATCH}.${quneiform_VERSION_TWEAK}, (c) 2021-${BUILD_YEAR} ${PUBLISHER}</string>

	<key>CFBundleIconFile</key>
	<string>app-logo.icns</string>

  <key>LSApplicationCategoryType</key>
  <string>public.app-category.productivity</string>

	<key>CFBundleIdentifier</key>
	<string>${PROGRAM_RDNS_IDENTIFIER}</string>

	<key>CFBundleLongVersionString</key>
	<string>${quneiform_VERSION_MAJOR}.${quneiform_VERSION_MINOR}.${quneiform_VERSION_PATCH}.${quneiform_VERSION_TWEAK}, (c) 2021-${BUILD_YEAR} ${PUBLISHER}</string>

	<key>CFBundleName</key>
	<string>${CMAKE_PROJECT_NAME}</string>

	<key>CFBundlePackageType</key>
	<string>APPL</string>

	<key>CFBundleShortVersionString</key>
	<string>${quneiform_VERSION_MAJOR}.${quneiform_VERSION_MINOR}.${quneiform_VERSION_PATCH}.${quneiform_VERSION_TWEAK}</string>

	<key>CFBundleVersion</key>
	<string>${quneiform_VERSION_MAJOR}.${quneiform_VERSION_MINOR}.${quneiform_VERSION_PATCH}.${quneiform_VERSION_TWEAK}</string>

	<key>NSHumanReadableCopyright</key>
	<string>Copyright 2021-${BUILD_YEAR} ${PUBLISHER}</string>

	<key>NSPrincipalClass</key>
	<string>wxNSApplication</string>

	<key>LSMinimumSystemVersion</key>
	<string>${CMAKE_OSX_DEPLOYMENT_TARGET}</string>
</dict>
</plist>
